# Jupyter Notebooks Directory

This directory is reserved for exploratory data analysis (EDA), meteorological diurnal cycle profiling, prototype feature experiments, and model tuning visualization.

## Guidelines
- Keep notebooks clean and modular.
- Extract reusable feature logic and pipeline code into `ml/` or `backend/app/`.
- Do not commit large inline binary chart outputs.
