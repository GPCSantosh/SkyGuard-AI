# Automation & Utility Scripts Directory

Utility scripts for database migration, dataset preprocessing, synthetic anomaly benchmarking, and local dev initialization.

## Available / Planned Scripts
- `init_db.py`: Database table setup and spatial station network bootstrapping.
- `run_synthetic_evaluation.py`: Automated benchmarking against injected ground-truth faults.
